#include "Drow.h"

// ctor
Drow::Drow() :
 Player(150, 25, 15, 0) {}

// strikes an enemy
void Drow::strike(Enemy &other) {
 return;
}

// gets struck by an enemy
void Drow::getStruckBy(Enemy &other) {
 return;
}
